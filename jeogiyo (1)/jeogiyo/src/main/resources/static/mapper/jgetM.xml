<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace='com.jpro.jmybatis.JgetMMapper'>
	
	<select id='chart' parameterType='Map' resultType='JgetMVo'>
		select * from 강남1호점
		where nal between  "${date1}" and "${date2}"
	</select>
	
 
</mapper> 